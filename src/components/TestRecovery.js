// Test component per verificare che il routing funzioni
import React from 'react';

const TestRecovery = () => {
    return (
        <div style={{ padding: '20px', color: 'white', textAlign: 'center' }}>
            <h1>🔥 RECOVERY TIMER FUNZIONA!</h1>
            <p>Se vedi questo messaggio, il routing sta funzionando correttamente.</p>
            <p>Ora possiamo verificare gli errori nel componente principale.</p>
        </div>
    );
};

export default TestRecovery;